# Core AI — NEW SDK (use 'from google import genai')
google-genai

# PDF extraction
pdfplumber
PyMuPDF

# Data processing
pandas
openpyxl

# Web scraping
requests
beautifulsoup4

# RAG pipeline
chromadb
sentence-transformers

# Web UI (Streamlit — alternative interface)
streamlit>=1.32.0

# FastAPI server (HTML frontend backend)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Utilities
tqdm
python-dotenv
colorama
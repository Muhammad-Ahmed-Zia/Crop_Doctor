# ─── Fasal Doctor — Deployment Requirements ───────────────────────────────────
# Used by Railway and HuggingFace Spaces.
# Excludes Phase 2 CNN packages (TensorFlow, scikit-learn etc.) — too large for
# cloud build limits and not needed for the RAG / Streamlit / FastAPI deployment.

# Core AI
google-genai

# RAG pipeline
chromadb
fastembed

# Web UI
streamlit>=1.32.0

# FastAPI server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Utilities
python-dotenv
colorama
requests
tqdm